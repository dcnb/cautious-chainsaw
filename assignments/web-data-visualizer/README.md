# 📘 Assignment: Web Data Visualizer

## 🎯 Objective

Students will learn to build an interactive web-based data visualizer using HTML, CSS, and JavaScript. They will load CSV data in the browser and create interactive charts to display the data visually.

## 📝 Tasks

### 🛠️ HTML Structure and CSV Loading

#### Description
Create the basic HTML structure for your data visualizer and implement functionality to load and parse CSV files from the browser.

#### Requirements
Completed program should:

- Create an HTML page with a file input that accepts CSV files
- Use JavaScript to read and parse the CSV file when uploaded
- Display the raw data in a table on the page
- Show column headers and at least the first 10 rows of data


### 🛠️ Data Visualization with Charts

#### Description
Transform the loaded CSV data into visual charts that help users understand the data at a glance.

#### Requirements
Completed program should:

- Create at least two different chart types (e.g., bar chart, line chart, pie chart)
- Use either vanilla JavaScript (Canvas API) or Chart.js library for visualization
- Allow users to select which columns to visualize
- Include proper labels, titles, and legends for each chart
- Style the page with CSS to make it visually appealing and easy to use


### 🛠️ Interactive Features (Bonus)

#### Description
Add interactive features that enhance the user experience and allow for data exploration.

#### Requirements
Completed program should:

- Add filtering or sorting capabilities for the data table
- Include hover effects or tooltips on chart elements showing exact values
- Allow users to toggle between different visualization types
- Add a download button to save charts as images
